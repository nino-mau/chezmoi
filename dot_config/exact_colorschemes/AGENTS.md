This is where i store and generate my custom color schemes

The `generate.ts` build the colorschemes for the respective applications, it uses themes.json as a base

Then for theme to be applied, i use this script `~/.local/bin/set_theme`
