{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(git add:*)",
      "Bash(npm run *)",
      "Bash(bun run *)",
      "Bash(git ls *)",
      "Bash(ls*)",
      "Read(~/Code/**)",
      "Edit(~/Code/projects/**)",
      "Read(~/Code/projects/**)"
    ],
    "deny": [
      "Read(*.env)",
      "Bash(curl:*)",
      "Bash(rm *)"
    ],
    "ask": [
      "Bash(gh pr create:*)",
      "Bash(git commit:*)"
    ],
    "defaultMode": "auto"
  },
  "model": "opus",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "fish -c 'notify \"Claude Code\" \"Ready for input\"'; if [ -n \"${TMUX_PANE:-}\" ]; then pane_tty=$(tmux display-message -p -t \"$TMUX_PANE\" '#{pane_tty}' 2>/dev/null); [ -n \"$pane_tty\" ] && printf '\\a' >\"$pane_tty\"; fi"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "AskUserQuestion",
        "hooks": [
          {
            "type": "command",
            "command": "fish -c 'notify \"Claude Code\" \"Question requires input\"'; if [ -n \"${TMUX_PANE:-}\" ]; then pane_tty=$(tmux display-message -p -t \"$TMUX_PANE\" '#{pane_tty}' 2>/dev/null); [ -n \"$pane_tty\" ] && printf '\\a' >\"$pane_tty\"; fi"
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "fish -c 'notify \"Claude Code\" \"Permission required\"'; if [ -n \"${TMUX_PANE:-}\" ]; then pane_tty=$(tmux display-message -p -t \"$TMUX_PANE\" '#{pane_tty}' 2>/dev/null); [ -n \"$pane_tty\" ] && printf '\\a' >\"$pane_tty\"; fi"
          }
        ]
      }
    ]
  },
  "effortLevel": "high",
  "tui": "fullscreen",
  "theme": "dark-ansi",
  "editorMode": "vim"
}
