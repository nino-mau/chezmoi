session_name: exb
start_directory: $HOME/Code/exb
windows:
  - window_name: cmd
    panes:
      - shell_command:
          - cd $HOME/Code/exb/client/your-extensions/widgets

  - window_name: cmd
    panes:
      - shell_command:
          - cd $HOME/Code/exb/client/your-extensions/widgets

  - window_name: cmd
    panes:
      - shell_command:
          - cd $HOME/Code/projects

  - window_name: client
    panes:
      - shell_command:
          - cd $HOME/Code/exb/client
          - pnpm start

  - window_name: server
    panes:
      - shell_command:
          - cd $HOME/Code/exb/server
          - pnpm start
