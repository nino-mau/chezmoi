session_name: exb10
start_directory: $HOME/Code/exb10
windows:
  - window_name: cmd
    panes:
      - shell_command:
          - cd $HOME/Code/exb10/client/your-extensions/widgets

  - window_name: cmd
    panes:
      - shell_command:
          - cd $HOME/Code/exb10/client/your-extensions/widgets

  - window_name: cmd
    panes:
      - shell_command:
          - cd $HOME/Code/projects

  - window_name: client
    panes:
      - shell_command:
          - cd $HOME/Code/exb10/client
          - fnm use 16
          - npm start

  - window_name: server
    panes:
      - shell_command:
          - cd $HOME/Code/exb10/server
          - npm start
